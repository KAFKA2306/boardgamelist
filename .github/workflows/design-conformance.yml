name: Design conformance

on:
  pull_request:
    paths:
      - "design.config.json"
      - "design.lock.json"
      - "docs/stylesheets/**"
      - "docs/design-managed/**"
      - "mkdocs.yml"
      - ".github/workflows/design-conformance.yml"
  push:
    branches: [main]
    paths:
      - "design.config.json"
      - "design.lock.json"
      - "docs/stylesheets/**"
      - "docs/design-managed/**"
      - "mkdocs.yml"
      - ".github/workflows/design-conformance.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  design-conformance:
    uses: KAFKA2306/design/.github/workflows/conformance.yml@0a7a3720c37d908721bc29cc42bbc9b7d353df8f
